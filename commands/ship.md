---
description: Test, review, and commit changes
allowed-tools: Bash(git:*), Bash(npm:*), Bash(pytest:*), Bash(go test:*)
---

Run a full pre-commit workflow.

Steps:

1. Run tests
2. Review changes
3. Generate commit message
4. Ask confirmation
5. Commit and push
